<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>You're invited to <%= @organization.name %></title>
  <style>
    <%= render "shared/aixle_email_base_styles" %>
    <%= render "shared/aixle_email_invitation_styles" %>
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <%= render "shared/aixle_email_logo" %>

      <h1>You've been invited!</h1>

      <p>
        <span class="inviter"><%= @inviter.display_name %></span> has invited you to join
        <span class="org-name"><%= @organization.name %></span> on Aixle Insights.
      </p>

      <p>
        You've been invited as a <span class="role-badge"><%= @invitation.role %></span>
      </p>

      <div style="text-align: center;">
        <a href="<%= @accept_url %>" class="button">Accept Invitation</a>
      </div>

      <p>
        Or copy and paste this link into your browser:<br>
        <a href="<%= @accept_url %>" class="link"><%= @accept_url %></a>
      </p>

      <p class="expires">
        This invitation expires on <%= @expires_at.strftime('%B %d, %Y at %I:%M %p %Z') %>
      </p>

      <div class="footer">
        <p>
          If you didn't expect this invitation, you can safely ignore this email.
        </p>
        <p>
          &copy; <%= Time.current.year %> Aixle Insights. All rights reserved.
        </p>
      </div>
    </div>
  </div>
</body>
</html>
